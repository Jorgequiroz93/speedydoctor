<div class="search-box">
  <div class="search-container">
  <p class="search-bar-label">Find a doctor :</p>
    <%= form_tag doctors_path, method: :get, class: "d-flex" do %>
      <%= text_field_tag :query,
        params[:query],
        class: "form-control search-placeholder",
        placeholder: "Find a doctor by name, specialty, country, language..."
      %>
      <div class="search-button">
      <button type="submit" class="btn btn-success btn-search"><i class="fas fa-search me-3"></i>Search</button>
      </div>
    <% end %>
  </div>
</div>
